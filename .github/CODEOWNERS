# Propriétaire du code : @wbatchayon doit approuver toute modification avant un
# merge vers main. Il est automatiquement demandé en reviewer sur chaque pull
# request (sauf celles qu'il ouvre lui-même — GitHub interdit l'auto-revue).
#
# NOTE : sur un repo privé en plan gratuit, l'approbation n'est pas *imposée*
# techniquement (la protection de branche / les rulesets exigent GitHub Pro ou
# un repo public). Ce fichier garantit la *demande de revue systématique* ;
# l'autorisation effective des merges reste manuelle.
* @wbatchayon
